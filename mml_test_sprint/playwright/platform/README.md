# Playwright Platform Tests (placeholder)

This directory is reserved for any future spec-style Playwright test
files (`*.spec.py`, `*.spec.ts`) that target the platform layer. The
current branch wires platform tests into the Python `BaseModuleTest`
harness instead — see `mml_test_sprint/modules/platform/`.

If a parallel spec-style suite is ever added, this directory keeps
the layout consistent with peer-agent dirs (`freight/`, `data/`).
